# All non-ROS Python dependencies for every Kufibot node, combining the
# simulation (WSL/Ubuntu) and Raspberry Pi hardware requirement sets.
# Prefer the platform-specific build scripts instead of installing this file
# directly:
#   ./tools/ros2_build_sim.sh   (WSL/Ubuntu simulation, no hardware)
#   ./tools/ros2_build_pi.sh    (Raspberry Pi 5, real robot hardware)
-r requirements-sim.txt
-r requirements-pi.txt
